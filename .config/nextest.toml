# Nextest configuration for anno
# See: https://nexte.st/book/configuration.html

[store]
dir = "target/nextest"

# =============================================================================
# Default Profile
# =============================================================================

[profile.default]
# Retry failed tests once
retries = 1

# Mark tests as slow after 30s, terminate after 60s
slow-timeout = { period = "30s", terminate-after = 2 }

# Fail fast on first failure in CI
fail-fast = false

# Status level for output
status-level = "pass"

# =============================================================================
# CI Profile - stricter timeouts
# =============================================================================

[profile.ci]
# Avoid re-running expensive timeouts: if it times out once, it almost always
# times out again (and retries multiply CI energy/time).
retries = 0
slow-timeout = { period = "60s", terminate-after = 2 }
fail-fast = true
status-level = "all"

# =============================================================================
# Quick Profile - fast tests only
# =============================================================================

[profile.quick]
retries = 0
slow-timeout = { period = "10s", terminate-after = 1 }

# Filter out slow tests: property tests, eval harness, flaky/slow binaries.
# NOTE: nextest `test(/.../)` matches the test name; for integration tests, that name
# often does not include the filename/module. Excluding by `binary(...)` is more robust.
default-filter = "not test(/property/) and not test(/proptest/) and not test(/eval::/) and not test(/eval.*harness/) and not test(/backend_availability_matrix/) and not binary(offset_fuzz_tests) and not binary(randomized_matrix_ci) and not binary(combiner_variance) and not binary(compare_command) and not binary(cli_context_output) and not binary(backend_factory_parity) and not binary(backend_integration) and not binary(candle_ner_regression) and not binary(eval_integration) and not binary(e2e_cli_workflows) and not binary(e2e_backend_scenarios) and not binary(e2e_eval_workflow) and not binary(e2e_full_pipeline) and not binary(e2e_dataset_pipeline) and not binary(e2e_error_recovery) and not binary(e2e_unicode_internationalization) and not binary(e2e_serialization_roundtrip) and not binary(e2e_advanced_scenarios) and not binary(integration_corpus_management) and not binary(integration_output_formatting) and not binary(integration_similarity_edge_cases) and not binary(integration_track_merging) and not binary(joint_integration) and not binary(mention_ranking_property) and not binary(multilingual_diversity) and not binary(no_features) and not binary(offset_edge_cases) and not binary(unicode_offset_edge_cases) and not binary(property_backends) and not binary(property_track_consistency) and not binary(property_url_detection) and not binary(property_crossdoc_transitivity) and not binary(regression_tests) and not binary(regression_old_text_entities) and not binary(regression_url_false_positives) and not binary(temporal_entity_integration) and not binary(human_voice_agent_schema) and not binary(human_voice_agent_dataset) and not binary(alignment_kilogram_integration) and not binary(backend_error_contracts) and not binary(backend_llm) and not binary(backend_provenance_contracts) and not binary(backend_router) and not binary(coref_pipeline_integration) and not binary(coreference_tests) and not binary(dataset_command_cli) and not binary(dataset_loading) and not binary(dataset_parsing_validation) and not binary(dataset_subcommands_e2e) and not binary(dataset_toml_sync) and not binary(discontinuous_entity_tests) and not binary(discourse_proptest) and not binary(env_integration) and not binary(features_comprehensive) and not binary(graph_coref_integration) and not binary(graph_coref_property) and not binary(hierarchical_confidence_tests) and not binary(metal_contiguous) and not binary(task_mapping) and not binary(unicode_edge_cases) and not binary(w2ner_integration) and not binary(wisdom_commands_e2e) and not binary(calibration_proptests)"

# =============================================================================
# ML Profile - for ML backend tests
# =============================================================================

[profile.ml]
# ML tests are slow due to model loading
retries = 1
slow-timeout = { period = "180s", terminate-after = 3 }

# Limit parallelism to avoid OOM with multiple models
test-threads = 2

# =============================================================================
# Property Test Profile
# =============================================================================

[profile.property]
retries = 0
slow-timeout = { period = "120s", terminate-after = 2 }
test-threads = 4

# Only run property/fuzz tests
default-filter = "test(/property/) or test(/fuzz/) or test(/proptest/)"
